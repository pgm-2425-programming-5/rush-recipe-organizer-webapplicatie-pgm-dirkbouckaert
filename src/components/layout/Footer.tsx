const Footer = () => {
  return (
    <footer className="mt-auto flex items-center justify-center bg-emerald-700 px-6 py-4 text-sm text-slate-50">
      <p>Arteveldehogeschool {new Date().getFullYear()}</p>
    </footer>
  );
};

export default Footer;
