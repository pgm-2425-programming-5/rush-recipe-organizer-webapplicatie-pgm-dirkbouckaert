import Spinner from '@/components/common/Spinner';

const Loading = () => {
  return (
    <div className="my-6 flex h-full w-full items-center justify-center">
      <Spinner />
    </div>
  );
};

export default Loading;
